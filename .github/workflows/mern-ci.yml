name: MERN CI Pipeline
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: jetset/package-lock.json
      - name: Build Frontend
        working-directory: jetset
        env:
          REACT_APP_ENV: ${{ secrets.REACT_APP_ENV }}
        run: |
          npm ci
          CI=false npm run build
      - uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: jetset/build

  build-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: src/package-lock.json
      - name: Lint Backend
        working-directory: src
        run: |
          npm install --legacy-peer-deps
          npm run lint
      - uses: actions/upload-artifact@v4
        with:
          name: backend-build
          path: |
            src/**
            !src/node_modules/**
            !src/.env

  deploy-backend:
    runs-on: ubuntu-latest
    needs: [build-frontend, build-backend]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: Download backend artifact
        uses: actions/download-artifact@v4
        with:
          name: backend-build
          path: ./backend-build

      - name: Prepare directories on VM
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_KEY }}
          script: |
            sudo mkdir -p /var/www/backend
            sudo mkdir -p /var/www/frontend
            sudo chown -R $USER:$USER /var/www/backend  # ← so SCP can write without sudo
            sudo chown -R $USER:$USER /var/www/frontend

      - name: SCP backend to VM
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_KEY }}
          source: "backend-build/"
          target: "/var/www/backend"
          strip_components: 1

      - name: Start backend on VM
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_KEY }}
          script: |
            cd /var/www/backend          # ← matches SCP target
            npm install --production --legacy-peer-deps

            cat > /var/www/backend/.env << 'ENVEOF'
            NODE_ENV=production
            PORT=3000
            MONGO_URI=${{ secrets.MONGO_URI }}
            AMADEUS_API_KEY=${{ secrets.AMADEUS_API_KEY }}
            AMADEUS_API_SECRET=${{ secrets.AMADEUS_API_SECRET }}
            HOTELBEDS_API_KEY=${{ secrets.HOTELBEDS_API_KEY }}
            HOTELBEDS_SECRET=${{ secrets.HOTELBEDS_SECRET }}
            EMAIL_USER=${{ secrets.EMAIL_USER }}
            EMAIL_PASS=${{ secrets.EMAIL_PASS }}
            STRIPE_SECRET_KEY=${{ secrets.STRIPE_SECRET_KEY }}
            ENVEOF

            pm2 restart api || pm2 start index.js --name api --cwd /var/www/backend
            pm2 save

  deploy-frontend:
    runs-on: ubuntu-latest
    needs: [build-frontend, deploy-backend]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: Download frontend artifact
        uses: actions/download-artifact@v4
        with:
          name: frontend-build
          path: ./frontend-build

      - name: SCP frontend to VM
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_KEY }}
          source: "frontend-build/"
          target: "/var/www/frontend"
          strip_components: 1

      - name: Reload Nginx
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_KEY }}
          script: |
            sudo nginx -s reload
